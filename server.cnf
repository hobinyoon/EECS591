[generic]
# The storage limit in Bytes.
storage_limit=5242880

[distributed_replication_configuration]

# The maximum radius from the current server
r_max=100000000

# The maximum concurrent number of requests to the file
k=2
